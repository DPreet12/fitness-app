<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Sign In</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <link rel="stylesheet" href="/css/delete.css">
</head>
<body>

    <% if (alerts.success) { %>
        <% alerts.success.forEach(msg => { %>
           
          <div class="alert alert-success"><%= msg %></div>
        <% }) %>
      <% } %>
  
      <% if (alerts.error) { %>
        <% alerts.error.forEach(msg => { %>
         <div class="alert alert-danger"><%= msg %></div>
        <% }) %>
    <% } %>
    
    <header class="header">
      <nav class="navbar">
        
         <a href="/">Home</a>
          <% if(!currentUser) { %> 
            <a href="/auth/signup">Signup</a>
            <a href="/auth/login">Login</a>
          <% } else { %> 
            <a href="/app/logs">New Log</a>
            <a href="/app/allWorkout">All Workout Logs</a>
            <a href="/profile">Profile</a>
            <a href="/auth/logout">Logout</a>
          <% } %> 
        
      </nav>
    </header>


      <section class="contact" id="contact">
        <h2 class="heading">Delete  <span>Exercise</span></h2>

        <form action="/app/deleteExercise/<%= delId2._id %>/?_method=DELETE" method="POST">
            <p>Type: <%= delId2.type %></p>
            <p>Duration: <%= delId2.duration %></p>
            <p>Distance: <%= delId2.distance %></p>
            <p>Sets: <%= delId2.sets %></p>
            <p>Reps: <%= delId2.reps %></p>
            <p>Notes: <%= delId2.notes %>
            </p>
         
            <input type="submit" value="Delete" class="btn">
            </form>
    
    </section>
  
</body>
</html>